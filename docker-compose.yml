version: '3.8'

services:
  rabbitmq:
    image: rabbitmq:3-management-alpine
    container_name: synthpay_rabbitmq
    ports:
      - "5672:5672"   # Porta de comunicação (AMQP) usada pelo nosso código .NET
      - "15672:15672" # Porta do painel de administração (Interface Gráfica)
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq

volumes:
  rabbitmq_data: